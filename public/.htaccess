RewriteEngine On

# Jangan rewrite file/folder yang memang ada (css, js, gambar, dll)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Arahkan semua request ke index.php sebagai parameter "url"
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]

public/.htaccess